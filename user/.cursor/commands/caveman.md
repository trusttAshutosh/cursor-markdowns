---
description: "Optional tight replies - caveman lite (off for RCA/Plan)"
---

# Caveman lite (opt-in)

Enable **caveman lite** for this chat:

- No filler/hedging
- Keep articles and full sentences
- Code, paths, errors, API names stay exact

Stay on until user says `stop caveman`, `normal mode`, or `/caveman off`.

**Hard off** (normal prose) for: RCA / incident timeline, Plan gate (`/ticket-kickoff`), `/rca-logs`, bank clarifications, security/irreversible warnings.

If user wrote `full` or `ultra` after the command, honor that level instead of lite.
