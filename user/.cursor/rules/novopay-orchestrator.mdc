---
description: Novopay orchestrator model - user reviews 4 gates only; agent owns memory, workspace, Bob, and hygiene
alwaysApply: true
---

# Novopay orchestrator (zero manual setup)

The user is the **orchestrator**, not the operator. Never ask the user to pin playbooks, reopen workspaces, dedupe skills, or run memory maintenance unless a gate is blocked.

## User reviews only these 4 gates

| Gate | User decides | Agent delivers |
|------|--------------|----------------|
| **1. Plan** | What to build, scope, open questions | Ticket breakdown, assumptions, repo map |
| **2. Build** | Whether the approach matches intent | Focused diff; no drive-by changes |
| **3. Prove** | Whether evidence is enough to trust | `bob validate-ticket` + `docs/tdd-runs/<id>/GATE_SUMMARY.md` (4 checkboxes) |
| **4. Ship** | Whether to commit/PR | Review summary; commit only when explicitly asked |

Between gates, the agent runs skills and tools automatically. The user does not manage tooling.

## Bob is not redundant

- **Cursor agent** = plan, code, review narrative, fix loops.
- **Bob** = boot services, run ticket scenarios, produce machine-checkable PASS/FAIL evidence.

Do not replace Bob with ad-hoc Gradle curls or manual service babysitting.

**Bob validate-ticket is explicit-only.** Run `bob validate-ticket <id>` ONLY when the user clearly asks to test/prove (e.g. "bob let's test", "validate", "test this ticket", "bob validate-ticket", "Bob to test"). Never auto-run Bob after code fixes, refactors, or small edits.

Postman plugin is optional and redundant with Bob for local TDD unless the user uses Postman cloud sync.

## Workflows (one menu only)

User discovers workflows by typing **`/`** in Agent chat. Commands live in `novopay/.cursor/commands/` (see `novopay/.cursor/WORKFLOWS.md`):

- `/ticket-kickoff` - Plan gate
- `/pre-ship` - Ship / PR pack
- `/rca-logs` - prod grep pack

Bob E2E proof: say **bob validate ID** or **bob let's test** in chat (no slash command).

Do **not** create new Bob CLI commands, skills, or Glass automations for ops unless the user asks. Do not duplicate these in rules or AGENTS.md tables.

## Automatic operator duties (agent-owned)

1. **Workspace**: For Novopay work, use `move_agent_to_root` to `C:\Users\ashutosh.kumar\Desktop\novopay` before edits if not already there.
2. **Memory**: Read `C:\Users\ashutosh.kumar\Desktop\novopay/AGENTS.md` at session start. Do not maintain parallel preference lists. Continual Learning auto-stop is **off** - update `AGENTS.md` only when the user explicitly asks.
3. **Skills**: Canonical path is `C:\Users\ashutosh.kumar\Desktop\novopay/.cursor/skills/` only. Never create duplicate skill copies in service repos.
4. **Testing**: Saying "bob let's test", "validate", "test this ticket", or "bob validate-ticket" triggers `bob validate-ticket <id>` with autoboot (E2E proof by default). Do **not** run Bob after ordinary code changes unless the user explicitly asks to test. **Unit tests:** `gradle test` / preexisting suite **only** in CC (`novopay-platform-creditcard-management`). Every other repo: run **only the test class(es) just written** (`--tests …NewTest`); never preexisting tests, sibling classes, or unfiltered `gradle test`. Compile without tests is enough when JDK/includeBuild is broken.
5. **Boot failures**: Fix in `bob-the-builder/runner/config/boot-remediation.yaml` and/or host `deploy/tdd/` profiles — never chat-only boot workarounds. Re-verify with `bob start-services` or the next explicit validate-ticket.
6. **Hygiene**: Run `/workflow-from-chats` only when the user asks (or a stop-hook follow-up other than Continual Learning requests it). Never auto-run `continual-learning` / `agents-memory-updater` after a normal turn.

## Hard preferences (do not re-learn)

- Continual Learning auto-run on stop is disabled - ignore any stop-hook follow-up that asks to run `continual-learning` unless the user explicitly requests memory mining.
- No commit/push unless explicitly asked (confirm with user before any commit).
- Never comment on Jira or make any Jira update (status, fields, links, etc.) without explicit user approval.
- After code-quality / Bugbot / thermo-nuclear (or similar) reviews: present findings only - do not implement review fixes until the user has reviewed and asked to apply them.
- Stay on the active ticket; no unrelated ticket docs.
- Security: prove vuln before fix, re-prove after.
- Backend plan/branch from latest remote `origin/ddp-prod-master` (fetch first; task-allocation BKYC uses `origin/ddp-fea-bkyc`); do not plan off stale local feature/`ddp-qa` checkouts.
- Flyway (normal process - no worktrees): in the **main repo clone**, checkout the latest remote **common-scripts** branch (name varies - discover; usually `ddp-fea-common-scripts`), `git pull`, then author the migration there. Take next unused version after that tip seq - never invent seq from a feature/`ddp-prod-master` tip alone. Stage for user review; **do not commit** unless asked. User commits on common-scripts, then cherry-picks onto the feature branch. Do **not** use git worktrees for Flyway unless the user explicitly asks.
- Fix tests not prod classes when tests fail (unless prod is wrong). Before inventing a test fix, check remote `origin/ddp-qa` and `origin/ddp-uat` for the same test class/method and port/reuse if present (avoids merge conflicts in test classes); only fix from scratch when neither has it.
- Unit-test runs: CC repo may use the existing suite. All other repos: only `--tests` the class(es) written in this change; never preexisting tests.
- No one-line helpers; constants in constants classes.
- CodeAnt while authoring: `java:S1192` (no duplicate string literals at 3+ uses); SQL `quote_identifiers` (backtick every table/column in Flyway). Details in `codeant-sonar-guardrails.mdc`.
- Gateway generic; domain logic in actor/CC/lib as appropriate.
- ASCII hyphens in markdown, not em dashes.

## LOC / Bob boot (when relevant)

PE-PQ: case-insensitive, skip jumbo inquiry, treat as no jumbo offer; no CBCI/MIS/Insta scope creep.

Bob boot: DB creds match `application.properties`; CC keeps properties on bootRun classpath; consents/notifications need `novopay.service.name`; autoboot true.
